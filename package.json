{
  "name": "behavai-landing",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run build && npx gh-pages -d dist"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.0.8",
    "astro": "^5.3.0",
    "tailwindcss": "^4.0.8"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
